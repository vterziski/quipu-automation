name: Full Test Suite

on:
  schedule:
    - cron: '0 6 * * 1-5'
  workflow_dispatch:

jobs:
  api-tests:
    uses: ./.github/workflows/api.yml
    secrets: inherit

  web-tests:
    uses: ./.github/workflows/web.yml
    secrets: inherit
