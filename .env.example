# Copy to .env and fill in values.
# For local Docker testing, set REGION=local (see .env.docker.example for Docker setup).

REGION=local
API_TOKEN=your_personal_access_token_here
USER_EMAIL=admin@local.dev
USER_PASSWORD=changeme
